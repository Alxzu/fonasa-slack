services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: fonasa
      POSTGRES_USER: fonasa
      POSTGRES_PASSWORD: fonasa
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
